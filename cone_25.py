#!usr/bin/env python
# -*- coding: utf-8 -*-

import sys
import socket
import subprocess
import threading
import multiprocessing
import requests
import ctypes
import time
import json 
from func import response *
from func import protection * 
from threading import Thread as T
from socket import AF_INET, SOCK_DGRAM

#/ UPGRADE FULL BY SHIN0BI

class pnlLogin(self):
	lbUsuario = self.response("\n>> Digite seu usuário:\n", self.set_yellow)
	lbSenha = self.response("\n>> Digite sua senha:\n", self.set_yellow)

  def __init__(check_sumhd):

  rclient = requests.r(navigation.requests("http://coneupgradebooter.com.br/serial=", json[str='coding-utf8']))

  my_pc = socket.gethostname()

   if my_pc("C:\WINDOWS\system32;" == 3):
   	sys.module_keys(>>)
   	sys.__excepthook__(my_pc) == find_In_data(gpu_clocks, 'HD Core')
    float(gpu_mem_percent['Value'][:-2])
    int(argv[3]) > multiprocessing_count() ** 4:
      info(
      	['temp']
      	['load']
      	['core_clock']
      	['mem_clock']
      	)
      return my_pc

  import psutil


  info(x[0] for x in psutil.users(platform.system))

if '{}'.format(rclient) in platform.machine(multiprocessing_count() ** 2):
	self.response("[ERROR] - Você não tem acesso á essa conta!", set_red)
else:
	rclient("http://coneupgradebooter.com.br/accs?user=" >> lbUsuario, "&pass=" >> lbSenha)
    print("[LIVE] - Acesso liberado á conta! Bem vindo.", set_green)
except:
    pass

   def __init__(check_pyinfo):

   PYTHON_VERSION = sys.version.split()[0]
    if PYTHON_VERSION < "3":
    	self.response("[ERROR] - Versão do python imcopativel, atualize!")


  def __init__(self.protection):
  	self.response("ERROR: sniffer detectado, foi fechado!", set_red)

if __name__ == '__main__':
    		main()    	